server:
      port: 8087
spring:
  application:
    name: e-commodity-provider
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://root:123456@localhost:7776/eureka/

dm-user-provider:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule